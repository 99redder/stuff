{
  "name": "stuff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "14.0.2"
  }
}
